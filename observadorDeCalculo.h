#ifndef OBSERVADORDECALCULO_H
#define OBSERVADORDECALCULO_H

#include <padroes/observer.h>
class ObservadorDeCalculo: public Observer
{
    public:
        ObservadorDeCalculo();
        virtual ~ObservadorDeCalculo();

    protected:

    private:
};

#endif // OBSERVADORDECALCULO_H
